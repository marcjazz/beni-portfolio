import { ArrowUpRight } from 'lucide-react'
import { Separator } from '@/components/ui/separator'
import { publications } from '@/lib/site-data'
export default function Publications() { return <section id="publications" className="bg-slate-50 py-24"><div className="mx-auto max-w-7xl px-5 lg:px-8"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-teal-700">Publications & travaux</p><h2 className="mt-4 text-4xl font-bold tracking-tight text-slate-950 sm:text-5xl">Quelques sujets qui m’animent.</h2></div><p className="max-w-sm leading-7 text-slate-500">Des projets sélectionnés à la rencontre de la clinique et de la statistique.</p></div><div className="mt-12">{publications.map((item, i) => <div key={item.title}><a href={item.href} className="group flex items-start justify-between gap-6 py-7"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">0{i + 1} / Projet sélectionné</p><h3 className="mt-3 text-xl font-semibold text-slate-900 group-hover:text-teal-700">{item.title}</h3><p className="mt-2 text-slate-500">{item.description}</p></div><ArrowUpRight className="shrink-0 text-slate-400 transition group-hover:-translate-y-1 group-hover:translate-x-1 group-hover:text-teal-600" /></a>{i < publications.length - 1 && <Separator />}</div>)}</div></div></section> }
