import Image from 'next/image'
import { Activity, BarChart3, Database } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { skills } from '@/lib/site-data'
const icons = { BarChart3, Activity, Database }
export default function SkillsGrid() { return <section id="skills" className="bg-slate-50 py-24"><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[.18em] text-teal-700">Compétences</p><div className="mt-4 flex flex-col justify-between gap-5 md:flex-row md:items-end"><h2 className="max-w-2xl text-4xl font-bold tracking-tight text-slate-950 sm:text-5xl">Des outils au service de questions cliniques concrètes.</h2><p className="max-w-sm text-slate-500">Une approche structurée, de la problématique de recherche à la restitution finale.</p></div><div className="mt-12 grid gap-5 md:grid-cols-3">{skills.map((skill) => { const Icon = icons[skill.icon as keyof typeof icons]; return <Card key={skill.title} className="border-slate-200 bg-white transition duration-300 hover:-translate-y-1 hover:shadow-lg"><CardHeader><div className="grid size-12 place-items-center rounded-xl bg-teal-50 text-teal-700">{skill.image ? <Image src={skill.image} alt="" role="presentation" width={28} height={28} className="size-7" /> : <Icon />}</div><CardTitle className="mt-4 text-xl">{skill.title}</CardTitle></CardHeader><CardContent className="flex flex-wrap gap-2">{skill.items.map(item => <Badge key={item} variant="secondary" className="bg-slate-100 text-slate-700">{item}</Badge>)}</CardContent></Card> })}</div></div></section> }
